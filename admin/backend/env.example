# Admin Backend Environment Variables

# Server Configuration
ADMIN_PORT=4001
NODE_ENV=development

# Database (Same as main backend)
DATABASE_URL="mongodb://localhost:27017/listup"

# JWT (Same secret as main backend)
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# CORS Origins
FRONTEND_URL=http://localhost:3001
MAIN_FRONTEND_URL=http://localhost:3000

# Email Configuration (Optional - for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Admin Configuration
ADMIN_EMAIL=admin@listup.ng
ADMIN_NAME=ListUp Admin

# Cloudinary Configuration (for image uploads)
CLOUD_NAME=your-cloudinary-cloud-name
CLOUD_API_KEY=your-cloudinary-api-key
CLOUD_API_SECRET=your-cloudinary-api-secret
